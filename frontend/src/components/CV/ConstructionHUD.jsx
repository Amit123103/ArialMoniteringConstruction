import React from 'react';

// AR-style overlay component - currently merged into canvasRenderer for performance,
// but kept as a stub if React-based HUD elements are needed atop canvas.
const ConstructionHUD = () => {
    return null;
};

export default ConstructionHUD;
